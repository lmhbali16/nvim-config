# nvim-config

This repo contains my current neovim configuration. As a new nvim adapter, I will hopefully make changes over time to improve my experience with it.
Currently, the repo contains configuration for my Windows PC but once I have a Mac, I should add the Mac config too.

